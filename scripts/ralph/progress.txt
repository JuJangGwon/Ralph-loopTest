# Ralph Progress Log
Started: Wed, May  6, 2026  4:04:37 PM
---

## Codebase Patterns
- Server entry: `server/index.js` — CommonJS, dotenv loaded from project-root `.env` via `path.resolve(__dirname, '..', '.env')`
- Run server: `cd server && npm start` (typecheck via `npm run typecheck` = `node --check` over each .js file)
- CORS origin allowlist: `http://localhost:5173` (Vite client default)
- `app` exported from `server/index.js`; `app.listen()` only runs when invoked as main module — keeps app importable from tests
- Two `package.json`s expected: `server/` (CommonJS) and future `client/` (Vite). No root package.json yet.
- `.env.example` lives at project root, NOT inside server/ (single source of truth for both server and future client config)
- Benefits cache: `server/benefits.js` exposes `loadBenefits()` (boot-time fetch + pagination) and `getBenefits()` (sync accessor). Module-scoped `BENEFITS` array; fallback `[]` on error. `_setBenefitsForTest(list)` available for unit tests (US-003 will use it).
- Normalized benefit shape: `{ id, name, region, jobCondition, incomeCondition: { earnMinAmt, earnMaxAmt, earnCndSeCd, raw }, summary, applyUrl }` — region uses `zipCd` from API (numeric region code). Future scoring/UI must compare regions in the same code space, NOT raw Korean names.
- 온통청년 API: JSON endpoint `https://www.youthcenter.go.kr/go/ythip/getPlcy`, params `apiKeyNm/pageNum/pageSize/rtnType=json`, response `result.youthPolicyList` + `result.pagging.totCount`. Override via `YOUTH_API_URL` env if needed.
- When `YOUTH_API_KEY` empty, `loadBenefits()` short-circuits to `[]` with a warning — server still boots cleanly. Use this for offline dev.
- Test runner: `node --test <file>` (built-in, no deps). Pass an explicit file path — `node --test __tests__` resolves the directory as a CJS module on Windows and crashes (MODULE_NOT_FOUND). Run via `npm test` from `server/`.
- Scoring: `server/score.js` exposes pure `scoreBenefit(benefit, input)` and `recommend(benefits, input)`. Region-match treats `''/null/'전국'/'00000'/'003001'` as nationwide (+1); exact string equality is +3. Job match splits benefit.jobCondition on `,/|`, then substring fallback. Income +2 when min/max both null OR user income within [min,max]. Recommend filters score<1, sorts score desc, name asc tiebreak.
- POST /api/recommend handler in `index.js` 400s with `{ error: 'missing required fields: ...' }` when any of name/region/job/income absent (income==='' counts as missing). Otherwise returns `{ recommendations }`.
- Client root: `client/` — Vite 5 + React 18 + react-router-dom 6, scaffolded manually (no `npm create vite` to avoid interactive prompts). `package.json type: "module"`, JSX in `.jsx` files, dev port 5173 strictPort.
- Client routes wired in `client/src/App.jsx` (Routes/Route) under `BrowserRouter` from `main.jsx`. Pages live in `client/src/pages/*.jsx`. Use `data-route` attribute on placeholder pages for sanity checks.
- Client typecheck: `npm run typecheck` runs `node --check vite.config.js`. Extend script per file when adding TS/JS source files that need static syntax check (parallels server pattern).
- Client env: `VITE_API_BASE_URL` from `client/.env` (committed) + `client/.env.example`. Vite only exposes vars prefixed `VITE_`.
- Root `.gitignore` already covers `node_modules/` and `dist/` for both `server/` and `client/` — no client-specific ignore needed.
- Client state: `client/src/store/funnelStore.js` exports `useFunnelStore` (zustand). Fields `name/region/job/income` default `''`. Mutators: `setField(field, value)` and `reset()`. Funnel pages should call `useFunnelStore((s) => s.field)` for narrow subscriptions and `setField` for writes — never mutate state directly.
- Client typecheck pattern: append `&& node --check <path>` to the `typecheck` script for every new `.js` source file (parallels server). JSX files skipped — `node --check` cannot parse JSX.
- Client UI components: live in `client/src/components/*.jsx`. Inline styles only (no Tailwind/CSS modules wired yet). Funnel steps map: `['이름','거주 지역','직업','연소득']` (1-indexed) — keep this order in sync if a step is added/removed.
- `vite build` is the practical safety net for JSX syntax — typecheck script doesn't cover `.jsx`. Run `npm run build` after touching components even when typecheck passes.
- Mobile-first sizing: use `clamp(min, vw, max)` for fluid font sizes and `maxWidth: 480px` + `width: 100%` + `boxSizing: border-box` on cards. Pattern in `client/src/pages/Landing.jsx`. Reuse for funnel pages so layout stays consistent across breakpoints without media queries.
- Page navigation: import `useNavigate` from `react-router-dom`, then `const navigate = useNavigate()`; call `navigate('/path')` in handlers. Don't use `<a href>` for in-app links (full reload).
- Funnel page shell: reuse Landing pattern — `<main style={pageStyle}><section style={cardStyle}>` with `<ProgressBar current={N} total={4} />` at top, labeled inputs, then primary button. Disabled-button style toggle: `background: canProceed ? '#2563eb' : '#94a3b8'` + `cursor: pointer↔not-allowed` keeps visual affordance consistent across steps.
- Funnel store binding: `useFunnelStore((s) => s.<field>)` for value, `useFunnelStore((s) => s.setField)` for writes. Drive inputs via `onChange={(e) => setField('<field>', e.target.value)}`. Selectors with narrow slices avoid cross-field re-render.
- Validation rule used in US-008: `name.trim().length > 0` to gate '다음'. Future steps should pick analogous trimmed/non-empty checks consistent with this approach.
- Funnel back/next button row: flex with `gap: 12px`. '이전' button bg `#e5e7eb` text `#0f172a` always enabled. '다음' button reuses US-008 disabled-toggle style. Pattern in `client/src/pages/FunnelRegion.jsx` — reuse for US-010/US-011.
- Region values stored as Korean 시/도 names (matches future API translation layer flagged in US-003). 17-region list lives inline in `FunnelRegion.jsx` as `REGIONS` const. If region matching needs zipCd codes (server side), translate at POST boundary in US-012, not in the store.
- `<select>` placeholder pattern: first `<option value="" disabled>` plus store default `''` keeps the select unselected on mount and gates '다음' via `region.length > 0`.
- Radio group pattern (US-010): `<fieldset>`+`<legend>` wraps `<div role="radiogroup">`. Each option = `<label>` wrapping `<input type="radio" name="job">` so click anywhere on row toggles. Selected option highlighted via border `#2563eb` + bg `#eff6ff`; unselected `#cbd5e1`/white. `accentColor: #2563eb` themes the native radio dot. Don't autoFocus radios (focus-jump confuses keyboard users).

## 2026-05-06 - US-001
- Implemented Express server scaffold with dotenv, cors, axios deps
- Files changed:
  - .gitignore (new) - ignores node_modules, .env, dist/build
  - .env.example (new) - YOUTH_API_KEY=, PORT=3001
  - server/package.json (new) - express/cors/dotenv/axios deps, start + typecheck scripts
  - server/index.js (new) - Express app, CORS allows :5173, GET /health -> 200 {status:'ok'}, listens on PORT
- Verified: typecheck pass, boot pass, curl /health returned HTTP 200 {"status":"ok"}
- **Learnings for future iterations:**
  - dotenv must be loaded BEFORE requiring modules that read process.env
  - Project-root .env keeps single source of truth; server resolves it via path.resolve(__dirname, '..', '.env')
  - Use `if (require.main === module)` guard around app.listen so US-002+ can import the app for tests without binding the port
  - axios already installed at US-001 for US-002 reuse — don't reinstall
  - server/__tests__/ (US-003) will need a test runner; not yet added — pick node --test (built-in, no extra dep) when needed
---

## 2026-05-06 - US-002
- Implemented boot-time youth benefits loader with in-memory cache
- Files changed:
  - server/benefits.js (new) - loadBenefits() paginates Ontongchungnyeon API, normalize() maps API fields to internal shape, getBenefits() returns module-scoped array; fallback [] on error or missing key
  - server/index.js - import benefits module, add GET /api/benefits/count, await loadBenefits() before app.listen()
  - server/package.json - extend typecheck to also `node --check benefits.js`
  - .env.example - add YOUTH_API_URL with default endpoint
- Verified: typecheck pass, server boots with empty key warning, /health -> 200 {"status":"ok"}, /api/benefits/count -> {"count":0}
- **Learnings for future iterations:**
  - axios via `axios.get(url, { params, timeout })` keeps URL construction safe (encoding handled)
  - Module-scoped `let BENEFITS = []` + named export of loader/accessor is the singleton pattern in use — do NOT export the array directly (binding stales after reassignment)
  - Pagination uses `result.pagging.totCount` (note the typo "pagging" — that's the actual API field, not a bug)
  - For US-003 test isolation: import `_setBenefitsForTest` to seed the cache before scoring tests, no need to mock axios
  - Region matching (US-003) must reconcile API `zipCd` (numeric) vs UI 17 시/도 names (US-009) — likely need a code→name map; flag this when implementing US-003
---

## 2026-05-06 - US-003
- Implemented POST /api/recommend with pure scoring lib + node:test unit suite
- Files changed:
  - server/score.js (new) - isNationwide/matchRegion/matchJob/matchIncome/scoreBenefit/recommend
  - server/index.js - add `/api/recommend` POST handler with field validation (400 on missing) calling `recommend(getBenefits(), input)`
  - server/__tests__/score.test.js (new) - 15 tests covering nationwide tokens, region/job/income matchers, score aggregation, sort+filter+tiebreak, response shape
  - server/package.json - typecheck extended to score.js + test file; `test` script: `node --test __tests__/score.test.js`
- Verified: typecheck pass, npm test 15/15 pass, smoke test via in-proc fetch returned 200 with sorted recommendations and 400 on missing fields
- **Learnings for future iterations:**
  - `node --test <dir>` is broken on Windows Node 22 — pass file path or glob, NOT a directory. Future test files must be added to the `test` script explicitly (or switch to a glob pattern).
  - Region matching gap remains for US-009/US-012: API benefit.region is `zipCd` (numeric) but UI will send Korean 시/도 names. Either (a) translate UI names to zipCd before POST, or (b) extend `matchRegion` to accept either form via a map. Decide before US-009.
  - Income comparison treats min/max as same unit as user input (만원 per US-011 label). API `earnMinAmt/earnMaxAmt` units must be confirmed against real responses; current logic assumes raw numeric equality of units.
  - For future endpoints needing benefits cache in tests: import `_setBenefitsForTest` from `./benefits` and seed before requests — works without HTTP listening (use `app.listen(0)` for ephemeral port).
  - Validation pattern: collect missing field names in array, 400 with `{ error: 'missing required fields: a, b' }`. Reuse for future POST endpoints to keep error shape consistent.
---

## 2026-05-06 - US-004
- Scaffolded React + Vite client with router (manual, no `npm create vite`)
- Files changed:
  - client/package.json (new) - vite 5.4, react 18.3, react-router-dom 6.26, @vitejs/plugin-react 4.3; scripts dev/build/preview/typecheck
  - client/vite.config.js (new) - react plugin, port 5173 strictPort
  - client/index.html (new) - lang=ko, title 청년 혜택 추천, mount #root
  - client/src/main.jsx (new) - ReactDOM root with BrowserRouter
  - client/src/App.jsx (new) - 6 Routes: /, /funnel/name, /funnel/region, /funnel/job, /funnel/income, /result
  - client/src/pages/{Landing,FunnelName,FunnelRegion,FunnelJob,FunnelIncome,Result}.jsx (new) - placeholder data-route divs
  - client/.env (new) - VITE_API_BASE_URL=http://localhost:3001
  - client/.env.example (new) - same
- Verified: `npm install` clean (66 pkgs), `npm run typecheck` pass, `vite build` pass (159 kB gzipped 51.78 kB), dev server boots on :5173, `curl /` `curl /funnel/name` `curl /result` all 200
- **Learnings for future iterations:**
  - Manual scaffold > `npm create vite` for harness use — interactive prompts hang the agent. React-only deps are 5 packages, fast to write directly.
  - `strictPort: true` in vite.config.js makes port collisions fail fast instead of silently shifting to 5174 — important when server CORS allowlist is hardcoded to 5173.
  - SPA all routes return 200 because Vite dev server serves index.html for unknown paths — does NOT mean the React route component rendered. For real verification of route rendering, browser test required (not yet wired). Future UI stories (US-006+) should use the dev-browser skill if available, else flag manual verification needed.
  - `package.json` `type: "module"` is required so `vite.config.js` can use ESM import syntax. Don't accidentally drop it.
  - Client deps separated from server deps by design (two `package.json`s); never co-mingle. Root has no package.json.
---

## 2026-05-06 - US-005
- Added zustand global state store for funnel inputs
- Files changed:
  - client/package.json - added zustand@^4.5.7 dep, extended typecheck to `node --check src/store/funnelStore.js`
  - client/package-lock.json - regenerated (zustand + use-sync-external-store)
  - client/src/store/funnelStore.js (new) - useFunnelStore exposes { name, region, job, income, setField, reset }; initialState reused by reset
- Verified: typecheck pass, smoke test via `node -e "import('./src/store/funnelStore.js')..."` confirms initial state empty, setField updates target field, reset restores all four fields to ''
- **Learnings for future iterations:**
  - zustand v4 uses named export `create` from `'zustand'` (not default). v5 changes signature - stay on v4 unless reason to upgrade.
  - Hook pattern: `const name = useFunnelStore((s) => s.name)` for narrow subscriptions in funnel pages (avoids re-render storms when unrelated fields change). `useFunnelStore((s) => s.setField)` is stable ref - safe to use in deps.
  - For tests/non-React code, use `useFunnelStore.getState()` and `useFunnelStore.setState()` (or call action via `getState().setField(...)`) - same store instance.
  - `node --check` cannot parse JSX, so skip `.jsx` files in typecheck. Plain `.js` modules in `src/` are fine to add to the chain.
  - reset spreads fresh `initialState` every time so mutating any field cannot leak into the constant - pattern is intentional, don't shortcut to a frozen reference unless you also freeze.
---

## 2026-05-06 - US-006
- Added ProgressBar component for funnel steps
- Files changed:
  - client/src/components/ProgressBar.jsx (new) - props { current, total }; horizontal aria-progressbar track filled current/total, step-dot row (completed=green check, active=blue ring, pending=gray), label "<n>/<total> <stepName>" from STEP_LABELS
  - scripts/ralph/prd.json - US-006 passes:true with notes
- Verified: `npm run typecheck` pass, `npm run build` pass (39 modules transformed, 159 kB / gzipped 51.78 kB). Browser verification deferred — no dev-browser MCP wired; recommend manual visual check once funnel pages consume ProgressBar (US-008+).
- **Learnings for future iterations:**
  - Inputs clamp defensively: safeCurrent = clamp(1,total), safeTotal = max(1,total). Defends against 0/NaN when devs navigate to a funnel route directly.
  - Step labels live in `STEP_LABELS` inside ProgressBar.jsx. If a funnel step changes, update this array AND the route table in `client/src/App.jsx` together.
  - `node --check` skips `.jsx` so `npm run build` is the only pre-commit safety net for JSX syntax errors. Add `npm run build` to the per-iteration QA chain whenever a `.jsx` file changes.
  - Inline styles chosen because no css framework wired yet. If/when a styling solution is added (CSS modules / Tailwind / styled-components), this component is first refactor candidate — keep self-contained until then.
  - Component exports default function; consumers import as `import ProgressBar from '../components/ProgressBar.jsx'` from `pages/`. Verify relative path when wiring in US-008+.
---

## 2026-05-06 - US-007
- Built landing page replacing placeholder
- Files changed:
  - client/src/pages/Landing.jsx - <main>+<section> card, h1 title, p description, '시작하기' button calling navigate('/funnel/name')
  - scripts/ralph/prd.json - US-007 passes:true with notes
- Verified: `npm run typecheck` pass, `npm run build` pass (39 modules, 163.29 kB / 53.47 kB gzipped). Browser verification deferred — no dev-browser MCP available; recommend manual visual check on :5173 once dev server running.
- **Learnings for future iterations:**
  - Use `useNavigate()` from `react-router-dom` for in-app navigation in funnel buttons (US-008..US-011) — consistent with Landing pattern. Don't fall back to `<a>` (causes reload).
  - Mobile-first without media queries: combine `clamp(min, vw, max)` font sizes + `maxWidth: 480px` card + `width: 100%` button + `boxSizing: border-box`. Card auto-fits phones up to 480px, fluid scales beyond. Reuse for FunnelName/Region/Job/Income/Result.
  - Inline-style page boilerplate: `<main style={pageStyle}><section style={cardStyle}>...</section></main>` with `pageStyle.minHeight=100vh` + flex center. Funnel pages should follow the same shell so navigation feels uniform.
  - Bundle grew ~4 kB (159 → 163 kB) from inline styles + Landing markup; expected. Watch chunk size when funnel pages add similar boilerplate — consider extracting shared shell only if duplication becomes painful (US-012 is good time to evaluate).
---

## 2026-05-06 - US-008
- Built funnel step 1 (name input) replacing placeholder
- Files changed:
  - client/src/pages/FunnelName.jsx - Landing-style shell, ProgressBar(1,4), labeled text input bound to funnelStore.name via setField, '다음' button disabled when trimmed name empty, navigates to /funnel/region
  - scripts/ralph/prd.json - US-008 passes:true with notes
- Verified: `npm run typecheck` pass, `npm run build` pass (54 modules / 169.72 kB / 55.65 kB gzipped). Browser verification deferred — no dev-browser MCP wired.
- **Learnings for future iterations:**
  - Bundle jumped 6 kB (163 → 169 kB) once first funnel page imports zustand selector + ProgressBar — first-time-cost. Subsequent funnel pages (US-009..US-011) should add only marginal kB since deps already in graph.
  - Disabled-button visual: bg #94a3b8 + cursor not-allowed. Keep across funnel steps so users can see progress is gated.
  - `name.trim().length > 0` gate avoids whitespace-only inputs being accepted. US-009 (region) won't need trim (select dropdown), US-010 (job) won't either (radio), but US-011 (income) needs `Number.isInteger(n) && n >= 0` per acceptance criteria.
  - Selectors split across two `useFunnelStore` hooks (one for value, one for setField) is intentional — keeps each subscription narrow. setField reference is stable so safe.
  - autoFocus on first funnel input cuts a tap on mobile; consider reusing on FunnelRegion select / FunnelIncome input but NOT on radio groups (focus-jump can confuse keyboard users).
---

## 2026-05-06 - US-009
- Built funnel step 2 (region select) replacing placeholder
- Files changed:
  - client/src/pages/FunnelRegion.jsx - Landing-style shell, ProgressBar(2,4), labeled <select> bound to funnelStore.region via setField, placeholder option `value=''` disabled, 17 시/도 entries, '이전'→/funnel/name, '다음' disabled until region selected then →/funnel/job
  - scripts/ralph/prd.json - US-009 passes:true with notes
- Verified: `npm run typecheck` pass, `npm run build` pass (54 modules / 171.43 kB / 56.05 kB gzipped). Browser verification deferred — no dev-browser MCP wired.
- **Learnings for future iterations:**
  - Bundle delta 1.7 kB (169.72 → 171.43 kB) — second funnel page only adds page-specific markup since zustand+ProgressBar already in graph. Confirms US-008 prediction. Expect similar deltas for US-010/US-011.
  - 17 시/도 list inline in FunnelRegion.jsx. If a zipCd↔name map becomes necessary for US-012 server scoring, factor list into `client/src/data/regions.js` with `{ name, zipCd }` shape — single source for both UI and POST translation.
  - `<select>` default `''` + first option `value=''` `disabled` is the canonical "placeholder" pattern — keeps store default consistent with empty-string convention. Don't initialize store fields to `null` for selects.
  - Did NOT autoFocus the select — picker UX on mobile would auto-pop the wheel, surprising users. autoFocus reserved for text inputs only (US-008, US-011).
  - Two-button row pattern (이전+다음 in flex row, gap 12px) is now established. US-010/US-011 should reuse identical layout so the funnel feels uniform.
---

## 2026-05-06 - US-010
- Built funnel step 3 (job select) replacing placeholder
- Files changed:
  - client/src/pages/FunnelJob.jsx - Landing-style shell, ProgressBar(3,4), <fieldset>+<legend>직업, role=radiogroup containing 5 <label>+<input type=radio> rows for 대학생/취업준비생/재직자/창업자/무직 bound to funnelStore.job via setField. Selected row gets blue border #2563eb + bg #eff6ff. accentColor #2563eb on inputs. '이전'→/funnel/region, '다음' disabled until job.length>0 then →/funnel/income.
  - scripts/ralph/prd.json - US-010 passes:true with notes
- Verified: `npm run typecheck` pass, `npm run build` pass (54 modules / 173.29 kB / 56.37 kB gzipped). Browser verification deferred — no dev-browser MCP wired.
- **Learnings for future iterations:**
  - Bundle delta 1.86 kB (171.43 → 173.29 kB) — confirms ~2 kB per funnel page once shared deps in graph. US-011 should land near 175 kB.
  - Tap-target: full <label> wraps radio + text so entire 14px-padded row is clickable (better mobile UX than tiny native radio circle alone).
  - `name="job"` on all radios scopes the group so only one is selectable at a time (browser-native behavior; don't fight it with manual state-juggling).
  - Visual selection state via border + background change is a reusable affordance for future selectable lists; consider extracting if US-012 result cards need similar.
  - `accentColor` on input themes the native dot — cheaper than custom-painting checkmarks. Works in evergreen browsers; harmless fallback elsewhere.
---
